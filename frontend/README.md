# Blogger Platform — Frontend

React + TypeScript + Vite UI for the Blogger Platform API (`hometask_24/api`).

## Run

1. Backend: `pnpm start:dev` (project root)
2. Frontend: `cd frontend && pnpm install && pnpm dev`
3. Open http://localhost:5173

See `.env.example` for configuration.
